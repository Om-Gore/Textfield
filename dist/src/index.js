// React 17 syntax
import React from "react";
import ReactDOM from "react-dom";
import FlexColumnGrid from "Temp";
ReactDOM.render(React.createElement(FlexColumnGrid, null), document.getElementById("root"));
